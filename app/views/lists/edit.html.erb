<hi>編集画面</hi>

<%= form_with model: @List, url: update_list_path(@List.id), method: :patch  do |f|  %>
  
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>本文</h4>
  <%= f.text_area :body %>
  
  <%= f.submit '保存' %>
  
<% end %>